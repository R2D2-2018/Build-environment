language: c++
sudo: required

compiler:
  - gcc
  
python:
  - "2.7"

install:
  - sudo pip install lizard -q # Cyclomatic Complexity Check Tool

addons:
  apt:
    packages:
      - gcc-arm-none-eabi
      - cmake
      - valgrind

script: ./configure.sh && ./test.sh
