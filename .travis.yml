language: c++
sudo: required

compiler:
  - gcc
  
python:
  - "2.7"

install:
  - sudo pip install lizard -q # Cyclomatic Complexity Check Tool

addons:
  apt:
    packages:
      - cmake
      - valgrind

script: ./configure.sh && cd build/ && ctest -V
